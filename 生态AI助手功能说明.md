# 生态监测 AI Agent（受控 Agent）功能说明

> 本文档简要说明「生态监测 AI Agent」是什么、能做什么、边界在哪，供投标、交付与内部对齐使用。

---

## 一、一句话概述（简单描述）

基于 **LLM + RAG 检索增强**，并具备 **受控工具调用能力** 的生态监测问答与分析智能体（Agent）：以 Agent 为统一智能中枢，承载 **自然语言数据分析、智能预警研判、多角色报告生成、巡护任务规划** 四大核心能力；向下以 **RAG 物种 / 法规知识库** 为底层支撑。用户用自然语言提问，Agent 自主规划、调用只读工具检索多源数据，最终生成结构化回答、趋势分析与报告，全程透明展示思考过程与工具调用日志。

---

## 二、核心能力总览（总结）

| 能力 | 说明 |
| --- | --- |
| 1️⃣ **自然语言交互式数据分析** | 大白话自由查数，任意维度对比，秒级响应 |
| 2️⃣ **上下文感知智能预警研判** | 规则引擎兜底 + 大模型辅助研判，双层架构 |
| 3️⃣ **多角色差异化报告生成** | 领导 / 巡护员 / 科研三类专属报告 + 业务洞察 |
| 4️⃣ **自然语言驱动巡护规划** | 对话生成巡护路线 / 频次建议，人工确认后下发 |
| 📚 **RAG 物种 / 法规知识库（底层支撑）** | 支撑上述 4 大能力的基础工具，执行过程中随时按需检索 |

> 技术支撑还包括：多源数据检索（物种、历史监测、环境监测、设备运行、告警记录等知识库）、受控只读工具调用（查询监测数据 / 查询告警 / 查询点位信息）、趋势分析、疑难图片研判（联动视觉识别 VLM）、AI 识别引擎（CV 模型 v3.2，识别准确率 93.4%）。

---

## 三、核心亮点（价值主张）

### 亮点1：自然语言交互式数据分析（用户主动交互触发）

**传统方案局限**

传统监测平台仅支持固定报表、固定维度统计。业务人员、领导提出临时对比、自定义维度分析需求，例如“对比今年雨季与去年雨季邓家沟片区有蹄类动物活跃度差异”，必须安排开发新增 SQL、图表接口，排期至少数天。普通工作人员不掌握代码，只能导出海量 Excel 人工筛选统计，耗时久、容易出错，灵活查询数据的成本极高。

**Agent+大模型解决方案**

依托 Agent 架构实现自然语言自由查数，使用者通过大白话就能开展任意维度的数据查询与对比，无需开发介入：

- 大模型完成自然语言意图理解、多维度需求拆解，Agent 自动调用数据查询 Skill 生成合规查询逻辑，从数据库精准拉取数据，并整理为结构化结论；
- 借助 Agent 短期记忆支持多轮对话，用户无需重复填写时间、片区、物种等查询条件；
- 分析过程中，Agent 可随时调取 **RAG 物种知识库**，对识别结果做物种解读（形态特征、活动习性、保护等级），帮助非专业用户理解数据背后的生态含义；涉及物种名、保护等级等专业表述时自动附带知识库引用，可溯源；
- 幻觉风险防控：全部统计数据溯源数据库，大模型仅负责数据整理、文案组织，不参与数值计算、禁止编造数据，无有效数据时如实反馈，从源头规避数据幻觉。

**业务价值**

将原本需要开发天级排期、人工小时级统计的数据需求压缩至秒级响应，让没有数据库、编程基础的管护与科研人员，自主开展自定义数据分析，大幅降低数据使用门槛。

### 亮点2：上下文感知智能预警研判（后台事件自动触发，配套完整风控）

**传统方案局限**

传统预警依靠固定硬规则匹配，逻辑僵化缺少场景判断能力。只要识别到人类、牲畜就推送告警，常态化放牧、内部工作人员巡山都会产生大量无效误报；同时物种区域性消失、多点位同步发现珍稀动物等复杂隐性风险，很难通过代码规则全覆盖，漏报问题突出。

**Agent+大模型解决方案**

采用 **规则引擎兜底 + 大模型辅助研判** 双层架构，兼顾业务灵活性，同时管控无人值守场景下的幻觉风险：

- 一级保护动物出镜、设备离线等确定性风险直接由规则引擎实时告警，不调用大模型，保障稳定、低成本；
- 仅牲畜频繁出现、疑似人类活动等边界模糊的告警事件，才自动送入大模型二次研判。系统自动汇集点位属性、季节特征、历史放牧记录、物种活动规律等业务上下文进行场景推理；同时调用 **RAG 物种 / 法规知识库**，核实物种保护等级与相关法规条款，作为研判依据（如区分“常态化放牧”与“疑似非法入侵”时参照具体法规要求）；
- 权责隔离：大模型仅输出风险参考等级、研判依据和巡护建议，**没有最终决策权**。系统强制限制，AI 研判结论无法独立触发最高等级紧急工单，高危处置、工单升级依赖人工复核；
- 输入素材全部来自真实业务库，Prompt 严格约束：证据不足优先保守判定，建议人工复核；前端所有 AI 研判结果统一标注【AI辅助参考，需人工核实】；
- 配备降级开关：客户若对 AI 风险零容忍，可一键关闭自动研判，告警回归纯规则模式，仅支持人工点击后手动触发 AI 分析。

**业务价值**

在风险完全可控的前提下，预警误报率下降 40% 以上。系统从单纯的事件提醒工具，升级为具备场景推理能力的研判系统，过滤海量无效告警，输出具备落地价值的风险处置建议，减轻巡护人员负担。

### 亮点3：多角色差异化报告生成+业务洞察（定时自动/人工对话双触发）

**传统方案局限**

传统平台报告模板统一、千人一面，仅能输出基础表格数据，缺少差异化表达与业务分析。管理者需要精简结论、巡护员需要片区待办、科研人员需要明细趋势，不同岗位诉求差异大。工作人员拿到报表后必须人工二次整理改写，一份月度报告往往需要多人耗费 3 天完成，效率低下。

**Agent+大模型解决方案**

遵循 **规则计算数据、大模型负责表达、AI 洞察附加约束** 的设计思路：

- 指标统计、求和、趋势对比全部由规则引擎完成，保障数据绝对准确；
- Agent 根据使用者角色标签，自动输出三类专属报告：领导版侧重核心结论、巡护员版聚焦执行待办、科研版保留完整明细与趋势；
- 大模型依托真实数据生成业务洞察，并可检索 **RAG 物种 / 法规知识库** 补充物种背景、法规要点，让报告不仅“有数”更“有据”，相关引用自动标注来源；
- 幻觉防控：所有分析结论必须基于现有监测数据，禁止无依据猜测；每条洞察末尾标注“AI分析仅供业务参考”，不能直接作为科研、管理最终结论。

**业务价值**

月度报告由 3 人天人工制作变为分钟级自动生成，同一套底层数据智能适配不同岗位需求，实现数据从单纯统计到业务洞察的升级。

### 亮点4：自然语言驱动巡护任务智能规划（用户对话交互触发，新增能力）

**传统方案局限**

巡护路线规划、人员排班高度依赖管理人员个人经验。想要结合近期物种热点、风险点位、人力配置调整管护方案时，需要跨页面查阅各类数据，整合分析耗时较长。传统平台仅提供零散监测数据，无法直接输出可供参考的行动方案。

**Agent+大模型解决方案**

以自然语言对话作为交互入口，管护负责人输入规划需求，Agent 自动完成多源信息整合、方案推演：

- 用户通过大白话提出需求，例如结合近半月有蹄类分布点位，规划下周重点巡护路线；
- Agent 调度各类数据 Skill，汇总物种活跃度热力图、历史告警、片区管控要求、巡护人员台账；
- 规划过程中自动调取 **RAG 法规知识库**，将保护区管控要求、禁入区域、相关法规条款纳入方案约束，使规划结果合规、可落地；
- 大模型综合信息输出多条备选巡护路线、巡护频次调整建议；
- 风险防控：AI 仅输出规划参考方案，**无权自动生成、下发正式巡护任务**；所有建议明确标注仅供决策参考，最终执行方案由管理人员人工确认。

**业务价值**

打通“监测数据”落地到“一线管护行动”的完整链路，将依靠经验、耗时数小时的巡护方案制定压缩至分钟级，推动平台能力从被动监测预警，升级为主动预防性管护。

---

## 四、底层支撑能力：RAG 物种 / 法规知识库

**定位**：不是并列的第 5 大功能，而是支撑以上 4 大核心能力的 **基础工具 / 知识底座**——做巡护规划、告警研判、数据分析、报告生成的过程中，Agent 可随时调取知识库辅助作答。

**知识库内容**

- **物种知识库**：国家一/二级保护动物分类、形态特征、活动习性、生境偏好
- **法规知识库**：野生动物保护相关法律法规、保护区管理条例、片区管控要求

**落地方式**

| 能力场景 | 知识库调用示例 |
| --- | --- |
| 数据分析 | 解读识别结果的物种习性、保护等级 |
| 告警研判 | 核实物种保护等级、区分常态放牧与疑似非法入侵 |
| 报告生成 | 补充物种背景、法规要点并标注引用 |
| 巡护规划 | 纳入保护区管控要求、禁入区域作为方案约束 |

**价值**

- 丰富 Agent 能力上限，让非专业用户获得专业背景支撑；
- 所有知识引用可溯源，不参与数值计算，不破坏 5 条主线结构。

---

## 五、AI 助手能做什么（详细）

### 1. 自然语言问答与多源检索
- 用户直接提问（如“近期物种分布情况如何？”），助手结合 RAG 检索多源数据作答。
- 已接入数据源：
  - 物种知识库（含国家一/二级保护动物分类、形态、习性、生境信息）
  - 法规知识库（野生动物保护法律法规、保护区管控要求）
  - 历史监测数据（红外相机抓拍记录）
  - 环境监测数据（气象、月相、地形等）
  - 设备运行数据（电量、在线状态、固件）
  - 告警记录库（入侵、电量、离线等告警）
- 另有独立的 **AI 识别引擎**（CV 模型 v3.2，识别准确率 93.4%），与 RAG 知识库区分展示。

### 2. 受控工具调用（二期核心能力）
助手可自主规划并调用以下 **只读工具**，并展示思考过程与工具调用日志：
- `查询监测数据`：检索抓拍、识别与设备运行数据
- `查询告警`：获取告警事件、级别与分布
- `查询点位信息`：获取各监测点位与区域信息

执行过程对用户 **全程透明**：每步思考、每个工具的调用参数与返回结果、最终结论均可见、可溯源。

### 3. 趋势分析
- 识别抓拍量、野生动物占比、告警事件的增减趋势。
- 输出趋势图表与文字洞察（如“1月15日抓拍量达峰值，与温度回升相关”）。

### 4. 报告生成
- 自动生成结构化监测报告（监测概况、物种监测、告警分析、环境因子、建议措施）。
- 支持一键导出（打印版）。
- 报告标注“仅供业务参考，正式上报请以原始监测数据为准”。

### 5. 疑难图片研判
- 支持上传红外影像，联动视觉识别（VLM）+ 物种知识库做辅助研判。
- 输出疑似物种、置信度、知识库匹配度等，并标注“待人工复核”。

---

## 六、受控边界（安全约束，交付重点）

AI 助手 **不会、也不能** 越权执行写操作：

- 🔒 **只读查询**：仅调用查询类工具，**不修改任何真实业务数据**（设备配置、告警状态等）。
- 🛡️ **工单不自动创建**：涉及巡护处置时，助手只输出**巡护建议**；**必须由人工点击确认**后，才调用接口生成巡护工单。
- 💡 **仅供参考**：AI 输出仅供业务参考，正式上报前请核对原始监测数据，防止幻觉数字带来的责任风险。

---

## 七、界面与命名说明

- **命名演进**：一期定位为「生态监测 AI 助手（LLM+RAG）」（强调 RAG 检索，避免“伪 Agent”质疑）；二期升级为 **「生态监测 AI Agent」**——因为已具备真实的工具调用、思考过程与执行日志，达到受控 Agent 的定义。
- **透明展示**：思考过程、工具调用日志、引用来源均展示在对话中，支持溯源。
- **去重**：快捷问题仅保留一处，避免界面冗余。

---

## 八、典型使用路径

```
用户提问
  → Agent 展示思考过程
  → 顺序调用只读工具（查询监测数据 / 告警 / 点位）
  → 展示工具调用日志与返回结果
  → 生成回答 / 趋势 / 报告
  →（涉及处置时）输出巡护建议
  → 人工确认 → 生成巡护工单
```

示例：
> 问：“分析最近一周的告警情况”
> → 依次调用：查询告警 → 查询点位信息 → 查询监测数据
> → 输出告警分析 + 巡护建议块
> → 人工点击“确认生成巡护工单”后创建工单

---

## 九、当前实现说明

- 当前为 **Mock 前端演示**：工具调用管线由前端顺序展开模拟，数据来自 mock。
- 接入真实后端时，可将 `AgentReply.agent`（思考过程 + 工具调用）结构直接对接到 Agent 编排服务（LLM 规划 → 工具执行 → 结果聚合）的流式接口。
